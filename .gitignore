# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
venv/
.venv/
.pytest_cache/

# Environnement / secrets
.env
.env.*
!.env.example

# Base de données locale (SQLite)
*.db
*.sqlite
*.sqlite3

# Logs applicatifs (RotatingFileHandler, voir app/logging_config.py)
logs/

# Frontend (Vue.js / Vite)
node_modules/
dist/
.vite/

# Fichiers de travail Claude Code (conservés localement, non publiés sur le repo)
screenshots/

# Documents de travail et de conception (dépôt public, 2026-08-30) : seul
# README.md (racine ou frontend/) reste versionné, tout le reste est local.
# Remplace les règles ponctuelles précédentes (CLAUDE.md, plan-etape*.md,
# cahier-des-charges-...md, etapes-roles-...md), désormais couvertes ici.
*.md
!README.md

# Éditeurs / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
